# =============================================================================
# OpenClaw 环境配置
# =============================================================================
# 复制此文件为 .env: cp .env.example .env
# 然后编辑填入你的配置
# =============================================================================

# -----------------------------------------------------------------------------
# 必填配置
# -----------------------------------------------------------------------------

# Gateway 认证 Token（必须修改）
# 生成命令: openssl rand -base64 32
OPENCLAW_GATEWAY_TOKEN=change-this-to-a-secure-token

# -----------------------------------------------------------------------------
# 可选配置
# -----------------------------------------------------------------------------

# API Keys（根据你的需求填写）
# ANTHROPIC_API_KEY=sk-ant-api03-xxx

# 端口配置（一般不需要修改）
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790

# 绑定地址:
#   lan       - 监听所有内网 IP（推荐，用于网络访问）
#   localhost - 仅本地访问
#   0.0.0.0   - 监听所有网络接口
OPENCLAW_GATEWAY_BIND=lan

# Docker 镜像（一般不需要修改）
OPENCLAW_IMAGE=openclaw:local
